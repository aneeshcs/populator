# Core. Tested against the NCAR `credit` conda env:
#   /glade/work/acsubram/conda-envs/credit/bin/python
# (torch 2.10+cu128, xarray 2026.x, einops 0.8, timm 1.0). Versions below are
# lower bounds; the env already satisfies them.
torch>=2.4
numpy>=1.24
xarray>=2024.0
netCDF4>=1.6
dask>=2024.0
einops>=0.8
timm>=1.0
pyyaml>=6.0
tqdm>=4.66

# Optional (logging / plotting / tests)
tensorboard>=2.15
matplotlib>=3.8
pytest>=8.0
